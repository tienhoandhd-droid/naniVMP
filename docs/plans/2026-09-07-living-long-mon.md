# Living Long Môn painting

User authorizes creative implementation and deployment. Extend ongoing Overview performance work with actual organic swimming, individual differences even for identical species, short playful pursuit, loose groups that dissolve, flower visits/rest, and size reflecting existing importance. Preserve data, chronological view, first-viewport whole painting, keyboard and click access.

Research: Craig Reynolds https://www.red3d.com/cwr/boids/ describes local separation, alignment and cohesion producing emergent schooling. Adapt these aesthetically, not as a claim of biological realism. Use a bounded pure simulation (seed injectable in tests, fresh session seed in UI), continuous velocities and limited turning/acceleration, independent per-fish personality and timed behavior transitions wander/school/pursue/flower/rest. Avoid synchronized looping, instant heading flips, permanent knots, and perimeter pinning. Inspect existing artwork to choose flower attraction zones. Existing source importance must determine monotonically increasing bounded visual size; never infer importance from deadline or random seed. Missing importance uses neutral size.

Architecture: keep longMonRaceModel authoritative and untouched. New pure organicSwimModel plus useOrganicSwim hook owned by fish implementer. One throttled requestAnimationFrame loop (max30fps), no per-frame React state, transform-only DOM writes, cached elements/bounds; bounded local-neighbor work with spatial grid or capped neighbor candidates. Separate heading transform wrapper from sprite/body animation and labels. Paused/hidden/reduced-motion must stop scheduling; resume without time jump. Pointer over individual fish freezes it for selection, keyboard focus freezes it; hovering empty pond must not freeze entire school. Cleanup observers/listeners/RAF on unmount/mode change. Resize stays within visible bounds, density150 remains practical. Date positions unchanged. Shared state implementation remains isolated from Overview extraction.

File ownership: fish worker only LongMonRace.tsx, long-mon-race.css, longMonPresentation.ts, new organicSwimModel.ts/useOrganicSwim.ts, new unit and E2E swim tests. Primary owns Overview, route budget, release/workflow and integration. No concurrent dist builds; worker requests primary preview for browser tests.

RED/GREEN: failing pure contracts for travel bounds, smooth turns, varied identities/trajectories, behavior transitions and importance monotonicity; then implementation. Browser proves actual displacement, differing directions, empty-pond hover continues, pause/focus/click/reduced-motion, date fixed positions, dense150 and mobile contained. Verify no original data mutation and clean teardown. Independent review required; primary inspects all diffs and reruns tests/build/a11y and existing Long Mon presentation suite. Rollback these isolated presentation changes with a revert; no database migration. Final CI and public artifact/health verification after authorized main push.

Art inspection: original silk scene has lotus upper-right near (75%,23%) and lower-left near (17%,76%); visit nearby water-side points rather than arbitrary center targets. Keep swimming away from the painted mountains/banks, with soft steering inside the water region rather than rectangle edge sliding. Heading follows actual pixel-space velocity on the 2:1 canvas. Preserve the same simulation state when pausing/resuming; new session seed must not cause jumps on pause. Hover and keyboard focus locks are independent.

Pending user business clarification: existing stage model assigns exactly one species; report completion and canonical VMP completion are separate flags. User questions whether these should be distinct workflow stages. Do not merge canonical completion rules without the answer; this affects all metrics/deadline warnings and exceeds a presentation-label change. Clearly describe report-done-but-VMP-not-recorded as a data state, not an assumed extra mandatory process.

Primary-only clarification: update three stage metadata labels to say what remains incomplete; report-done becomes “Chưa ghi nhận VMP” with full text explicitly identifying report completion. Exclusive classifier and source completion flags stay unchanged pending business answer. No species or source record changed.

Measured dense performance checkpoint: at 150 fish, a 3s local browser sample recorded TaskDuration 2.057s, ScriptDuration 0.070s, RecalcStyleDuration 1.027s, LayoutDuration 0. Inherited CSS-variable writes still invalidate too much descendant styling. Complete the planned transform-only design with a cached heading wrapper and direct local transform writes: position wrapper translates, heading wrapper rotates/mirrors, existing body CSS owns responsive density scaling. Do not mutate shared date properties or perform per-frame descendant queries. Re-measure the same 150-fish scene after this correction. Uniform density scale must precede source importance; random per-ID size jitter cannot invert importance ordering.

Final dense A/B: direct transforms alone cut recalculation but 150 redundant body/wake CSS animations remained expensive. Same-scene 3s probe: Task 2.110s/RecalcStyle .755s versus .702s/.248s with only dense organic body/wake animations disabled (Layout 0 in both). Apply that scoped rule; keep real steering/heading/hover gestures and sparse motion. Date presentation uses uniform model.densityScale × .88 before importance/1.14, so source importance is strictly ordered across IDs and all sprites remain within the original collision envelope. Authoritative placement/classifier stays unchanged.
