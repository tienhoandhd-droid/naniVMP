# Load overview analysis on demand

User requests measured performance upgrades and already authorizes main deployment. Primary reviewed App imports, component usage, CSS selectors and current budgets. Overview charts are eager although Today is the default route. overview-executive.css has shared global filter rules in its first section and overview-only analysis thereafter; analysis.css has shared registry rules and MUST remain eager.

Architecture: extract the existing Overview component unchanged into features/overview/Overview.tsx, use a pages/OverviewPage.tsx adapter to load its dedicated analysis CSS, lazy+memo under existing Suspense/retry/ScreenGuard. Keep exact acts, access, setView props and personal/team scope handling in App. Keep shared global-filter CSS eager; move only overview-specific CSS rules to overview-analysis.css without editing selectors. Add intent prefetch with existing desktop/SaveData guards and an explicit Overview route budget. No database/cache/authorization redesign.

Tasks: primary implements extraction, CSS split and budget/prefetch contracts. Independent test worker owns a new browser measurement/regression script, capturing baseline before primary changes and before/after cold Today/Overview resource bytes/ready time with identical mocked data, plus route navigation/chart detail/return/filter preservation. No concurrent preview builds. Independent reviewer checks final diff while primary verifies.

RED/GREEN: initial-page resource/css contract shows Overview code/style eager; after extraction requires no Overview chunk or analysis selector in cold Today assets, load-on-navigation and working charts. Existing metric/scope tests must remain correct. No business behavior changes are intended; restore focus/loading/access guarantees from existing shell. Final checks: unit contracts, typecheck/build/drift/budgets, browser measurement, Today/overview personal scope, axe and shell. Rollback a single extraction commit. Deploy after review and required CI, verify public artifacts/health. Report transferred bytes separately from variable lab timing; no invented field Core Web Vitals claims.
