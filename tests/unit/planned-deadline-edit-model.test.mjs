import test from "node:test"; import assert from "node:assert/strict";
import { canPresentPlannedDeadlineEdit, plannedSnapshot, protectedSnapshot, validatePlannedDeadlineDraft } from "../../src/features/timeline/plannedDeadlineEditModel.ts";
const before={deadline_protocol:"2026-01-01",deadline_validation:"2026-01-02",deadline_report:"2026-01-03",deadline_vmp:"2026-01-04"};
test("gate chỉ admin/qa_manager khi env đúng",()=>{assert.equal(canPresentPlannedDeadlineEdit("true","admin"),true);assert.equal(canPresentPlannedDeadlineEdit("true","qa_staff"),false);assert.equal(canPresentPlannedDeadlineEdit("false","qa_manager"),false)});
test("draft cần version reason confirmation, không xoá/đảo/noop",()=>{assert.match(validatePlannedDeadlineDraft(before,before,"x",true,7),/Không có/);assert.match(validatePlannedDeadlineDraft(before,{...before,deadline_protocol:null},"x",true,7),/Không được/);assert.match(validatePlannedDeadlineDraft(before,{...before,deadline_protocol:"2026-02-01"},"x",true,7),/thứ tự/);assert.match(validatePlannedDeadlineDraft(before,{...before,deadline_vmp:"2026-01-05"},"x",true,NaN),/phiên bản/);assert.equal(validatePlannedDeadlineDraft(before,{...before,deadline_vmp:"2026-01-05"},"x",true,7),null)});
test("snapshot đọc adapted rồi raw rồi legacy và đủ protected evidence",()=>{const a={dlProtocol:"2026-01-01",_raw:{dl_tham_dinh:"2026-01-02",deadline_report:"2026-01-03",dl_vmp:"2026-01-04",actual_protocol_date:"2025-01-01",tt_tham_dinh:"completed"}};assert.equal(plannedSnapshot(a).deadline_validation,"2026-01-02");assert.equal(protectedSnapshot(a).actual_protocol_date,"2025-01-01");assert.equal(protectedSnapshot(a).status_validation,"completed")});
